import { Styles } from 'src/types';

export const styles: Styles = {
  root: {
    width: '100%',
    height: '100%',
    fontSize: 128,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
};
