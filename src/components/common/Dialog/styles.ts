import { Styles } from 'src/types';

export const styles: Styles = {
  message: {
    marginTop: 10,
    marginBottom: 20,
    fontSize: 16,
  },
  buttonsRow: {
    display: 'flex',
    flexDirection: 'row',
  },
};
